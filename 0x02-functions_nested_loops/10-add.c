#include "main.h"

/**
 * add - calculate the sum of two integers
 *
 * @x: a parameter that take an integer
 * @y: a parameter that take an integer
 *
 * Return: The result of the addition
 */
int add(int x, int y)
{
	int z = x + y;

	return (z);
}
