#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a & @b: parameters that accepts the multiplied integers
 *
 * Return: the multiplied value
 */
int mul(int a, int b)
{
	int x = a * b;

	return (x);
}
